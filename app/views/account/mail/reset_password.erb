Hi,

We've received a request to reset the password for the account at this email address.

Click the link below to confirm your email. This will send you another email with your new password.

<%= Environment.config['hostname'] %>/account/password/reset?reset_token=<%= user.reset_token %>


<hr/>

This email is sent from <a href="<%= Environment.config['hostname'] %>">Scout</a>, a service of the <a href="http://sunlightfoundation.com">Sunlight Foundation</a>.

If you received this message in error, you can safely delete it.